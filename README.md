# aws-serverless-url-shortener
A serverless URL shortener built with AWS Lambda, DynamoDB, and API Gateway. It creates short links, redirects to original URLs, tracks clicks, supports expiration, and includes basic monitoring with CloudWatch.
